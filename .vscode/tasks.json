{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "ninja -C build && ./build/src/com.github.manexim.emoji-art",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}